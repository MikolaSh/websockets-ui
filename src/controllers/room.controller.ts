import { RoomService } from "../services/room.service.ts";
import { RoomView } from "../view/room.view.ts";

export class RoomController {
  constructor(roomService: RoomService, roomView: RoomView) {
    
  }
}